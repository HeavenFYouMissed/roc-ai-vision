# ROC AI Vision — Project Tree

> **Living document.** This file must be updated every time a file is created, renamed, or moved.
> Last updated: 2026-05-18 — **Amendment v8 landed** — Phase 8 desktop / window / region capture pipeline + Phase 4½.1 staging of operator's existing DirectML runtime + 5 game-detection model demo set. NEW files: `core/transport/include+src/desktop_capture.{hpp,cpp}` (~600 LOC Win32 GDI BitBlt screen / monitor / window / region capture; `DesktopCaptureKind` enum + `DesktopCaptureConfig` + `DesktopCapture` class with cv::VideoCapture-shaped API; `enumerate_monitors()` + `enumerate_top_level_windows()` helpers using EnumDisplayMonitors / EnumWindows; per-monitor DPI awareness via PROCESS_PER_MONITOR_DPI_AWARE_V2; optional cursor overlay via CURSORINFO+DrawIconEx; rolling EMA FPS estimate; optional downscale via cv::resize INTER_AREA); `dashboard/src/lib/stores/capture_sources_store.svelte.ts` (reactive client for /api/capture-sources + `synthesizeCliFlags()` helper); `third_party/runtime_dlls/{DirectML.dll, onnxruntime.dll, onnxruntime_providers_shared.dll}` (staged from C:\Users\heave\Desktop\Classic\). MODIFIED: `core/transport/CMakeLists.txt` (conditionally adds desktop_capture.cpp on WIN32; links Gdi32 + User32 + Shcore); `core/transport/include+src/mjpeg_server.{hpp,cpp}` (added `CaptureMonitorInfo` + `CaptureWindowInfo` + `CaptureSourcesSnapshot` + `list_capture_sources` handler in ApiHandlers; GET /api/capture-sources route + JSON serialisers); `core/main.cpp` (added 9 new CLI flags `--desktop-capture` / `--capture-monitor` / `--capture-window` / `--capture-region` / `--capture-fps` / `--capture-downscale` / `--capture-cursor`; added desktop_cap declaration in main; mutually-exclusive source open path: cv::VideoCapture OR DesktopCapture; conditional t_camera launch dispatches between camera_capture_thread and desktop_capture_thread; ApiHandlers.list_capture_sources wired to enumerate_monitors + enumerate_top_level_windows; desktop_cap.release() added to teardown; new desktop_capture_thread function defined alongside camera_capture_thread using same SPSC contract); `scripts/scan_onnx_to_manifest.py` (added comma between `name` and `roc_label` in the emitted class table line — critical for yaml-cpp validation; added [N, 6] / [N, 7] NMS-in-graph detection that emits REVIEW comments instead of mis-classifying as transposed yolov8); `dashboard/src/lib/components/drawers/SourcesDrawer.svelte` (full DESKTOP CAPTURE section: kind pills primary/monitor/window/region + live monitor and window dropdowns + region X/Y/W/H inputs + fps/downscale/cursor options + synthesised CLI command block with Copy button); `docs/build_directml.md` (added Option A documenting the pre-staged runtime DLLs at third_party/runtime_dlls/). STAGED MODELS (in `models/`): Apex-Heavy.{onnx,yaml}, CS2-body-small.{onnx,yaml}, Fortnite-Heavy.{onnx,yaml}, Universal-xl.{onnx,yaml} — auto-generated manifests from the operator's aimbot-workspace ONNX collection via scan_onnx_to_manifest.py; restored yolo26n-coco.yaml (Amendment v7 accidentally overwrote it during the --force regenerate; now contains the canonical 80-class COCO table). Hard guarantee: when `--desktop-capture` is anything other than `none`, the orchestrator opens a `DesktopCapture` instead of cv::VideoCapture and uses the SAME downstream SPSC queues; FOV crop pipeline applies identically. Prior amendment: 2026-05-18 — **Amendment v7 landed** — Production model-browser + hot-swap + FOV crop pipeline. NEW files: `scripts/scan_onnx_to_manifest.py` (bulk ONNX→YAML manifest auto-generator with ONNX graph introspection, head classification via output shape, COCO/ROC-humanoid/single-class pre-populated class tables, --force/--dry-run/--verbose flags), `dashboard/src/lib/stores/model_store.svelte.ts` (reactive REST client for /api/models + /api/model/active + POST swap), `dashboard/src/lib/stores/fov_store.svelte.ts` (FOV crop state + 500 ms diagnostics polling), `dashboard/src/lib/components/drawers/FovDrawer.svelte` (operator FOV control surface: enable/disable + cx/cy sliders + radius + follow-target toggle + live diagnostics block), `docs/build_directml.md` (DirectML EP install + verify + troubleshooting). MODIFIED: `core/vision_pipeline/include+src/inference_engine.{hpp,cpp}` (added `try_swap_model()` with mutex-guarded atomic session pointer swap + EP re-probe + cross-check vs new manifest; hot-path `run()` and `run_batched()` now hold the swap mutex for one Ort::Run duration so the swap thread waits at most one frame); `core/transport/include+src/mjpeg_server.{hpp,cpp}` (added `ApiHandlers` callback struct + JSON serialisers for `ModelInfo` and `FovSettings` + GET/POST routes for /api/models, /api/model/active, /api/fov + OPTIONS preflight + hand-rolled JSON body extractors); `core/main.cpp` (added `FovStore` mutex-guarded class + `resolve_crop()` clamping math + `enumerate_models()` + `resolve_model_name()` + `active_model_json()` + `inference_thread` extended with FOV crop application before engine.run() and bbox un-crop after; `tracker_thread` extended with Phase 7 follow-target EMA pushing chosen target's EKF pixel position into FOV centre; ApiHandlers registered with mjpeg_server right after start; added `--models-dir` CLI flag); `core/vision_pipeline/CMakeLists.txt` (added DirectML auto-detection branch: when -DDIRECTML_ROOT is set and DirectML.h/.lib/.dll are found, auto-enable -DROC_VISION_HAVE_DML_EP=ON, add includes, link DirectML.lib, copy DirectML.dll alongside onnxruntime.dll); `dashboard/src/lib/components/drawers/ModelsDrawer.svelte` (FULL REWRITE — hardcoded fake list replaced with live `modelStore` data; refresh button, error banner, active highlight, switch button per card disabled when broken/active/swap-in-progress); `dashboard/src/lib/components/shell/LeftRail.svelte` (added FOV drawer button with crosshair-in-square icon; expanded onDrawerToggle union type to include 'fov'); `dashboard/src/lib/components/NavigationShell.svelte` (imported FovDrawer + extended activeDrawer union type to include 'fov' + extended handleDrawerToggle union type); `dashboard/src/lib/components/destinations/OperateCanvas.svelte` (added `fovCropRectRef` SVGRectElement bound to `fovStore.settings.activeCrop*` so the dashed orange rectangle shows the EXACT inference crop region; started `fovStore.startPolling()` in onMount + stop in onDestroy); `dashboard/src/lib/components/biometrics/SessionStatsPanel.svelte` (Session column rewritten from horizontal-2-column-grid `dt`/`dd` (which clipped values when the long "Dir changes >= 45deg:" dt exceeded the narrow column) to vertical-stack per-stat with `.stat-label`/`.stat-value` — values are now ALWAYS visible regardless of column width). Hard guarantee: model swaps are FULLY ATOMIC (failure path keeps prior session active with explanatory error; success returns active model name + chosen EP); FOV crop is per-frame zero-copy via cv::Mat ROI; the inference thread holds swap_mtx_ for at most one Ort::Run call so swap latency bounded by inference frame time. Prior amendment: 2026-05-18 — **Amendment v6 landed** — new 5th destination `Biometrics` (Mouse Dynamics Capture, hotkey `5`). NEW files: `dashboard/src/lib/biometrics/{mouse_capture.svelte.ts,labels.svelte.ts,metrics.ts,export.ts}` (Pointer Lock + ring buffer + zero-alloc compute + JSONL/PNG export + label vocabulary), `dashboard/src/lib/components/biometrics/{TracePanel,SessionStatsPanel,SparklinePanel}.svelte` (3 reusable panel primitives), `dashboard/src/lib/components/destinations/BiometricsDestination.svelte` (7-section layout: collapsible top bar + 2 trace panels at 60% width + 4 metric column types at 40% — Session/Report rate/Velocity/Sign flip rate). MODIFIED: `controller_matrix.ts` (5th entry + `pulse` icon key), `LeftRail.svelte` (added `pulse` ECG-trace SVG path), `NavigationShell.svelte` (route branch + `destination:biometrics` keybinding), `keybindings/defaults.ts` (combo `5` registered). Binding spec: `dashboard_menu_architecture_2026.md` Amendment v6 (cardinality lock relaxed to 5 with one documented exception — Biometrics has self-contained data plane, hard-decoupled from `trackPool`/`actuatorPool`/`bgOdometryPool`/`telemetrySocket`); `UI_SPEC_SHEET.md` §16 (layout proportions, color tokens, state machine, JSONL wire format, hard constraints). Sign-flip rate panel intentionally the ONLY red-bordered element in the entire dashboard, serving as the AC-research alarm marker. Prior: 2026-05-18 — JS Ruckig-lite virtual cursor controller landed in `OperateCanvas.svelte` (Phase 6-part-A stand-in; UI_SPEC_SHEET.md §15). Prior: 2026-05-18 — Overlay stage pattern applied to `OperateCanvas.svelte` per `research_svg_overlay_alignment_2026.md`: `<img>` and `<svg>` now share a single `.canvas-stage` div with `aspect-ratio: VIEW_W / VIEW_H` + `overflow: hidden`, eliminating gutter spillover; `<img>` switched from `object-fit: contain` to `object-fit: fill`; `.layer-0-camera` wrapper removed; all stroked overlay primitives (FOV ring, trajectories, bboxes + halos, anchor crosshairs, velocity arrows, hierarchy edges, hover/selection rings, lock crosshairs, actuator raw polyline + clean path) now carry `vector-effect="non-scaling-stroke"`; FOV ring radius capped at `min(200 + α * 520, min(VIEW_W, VIEW_H) * 0.48)` as belt-and-suspenders guard; new `.cursor/rules/220-operate-canvas-stage.mdc` binds the stage-div pattern as a hard rule; new top-level `AGENTS.md` for external tooling; `UI_SPEC_SHEET.md` extended with §14. Prior: 2026-05-17 night — Polish Pass 2 + C++ MJPEG-over-HTTP server. NEW: `core/transport/include/roc/transport/mjpeg_server.hpp` + `core/transport/src/mjpeg_server.cpp` (multipart MJPEG streaming + /sources JSON + /health, loopback-only). `core/main.cpp` extended with `--mjpeg-port/--no-mjpeg/--mjpeg-quality` flags + camera-thread integration. Dashboard: `lib/stores/{focused_panel,rail_state}.svelte.ts` NEW. BBox label collision auto-stagger, Calibrate ←/→/S keyboard nav, left rail activity badges, Inspect right rail per-panel settings, [ hotkey for left rail toggle, Scenario→Profile auto-switch. Prior: Cockpit Rebuild Sessions 1-5 COMPLETE. New dashboard files: `lib/render/class_colors.ts`, `lib/stores/{render_settings,selection_store,profile_store,engage_state,event_log,overlay_mode}.svelte.ts`, `lib/components/destinations/{OperateCanvas,InspectDestination,CalibrateDestination,EngageDestination}.svelte`, `lib/components/overlays/{LayerVisibilityToolbar,ModelCard,ObjectCountBadge,TargetLockModal}.svelte`, `lib/components/panels/inspect/{HierarchyExplorerDrawer,PipelineGraphPanel,ModelInspectorPanel,TopicGraphPanel,EventLogDock}.svelte`. NEW: `workspace_blueprint/UI_SPEC_SHEET.md` (living UI/UX spec for external reviewers — design tokens, component tree, wire format, safety interlock logic, hotkey map, file reference). Scenarios expanded from 2 to 6 in `scenarios.ts`. Prior: Dashboard Shell Rebuild (post-Phase 5C). New: `lib/components/shell/{LeftRail,TopChrome,DiagnosticsStrip,CommandPalette}.svelte`, `lib/components/drawers/{ModelsDrawer,SettingsDrawer}.svelte`, `lib/keybindings/{defaults.ts,manager.svelte.ts}`; `NavigationShell.svelte` rewritten to 3-zone shell (left-rail + top-chrome + content viewport); `controller_matrix.ts` relabelled Operate/Inspect/Calibrate/Engage; `DomainPanelLayout.svelte` restyled (removed placeholder expansion text); `KinematicTrajectoryControllerPanel` + `HardwareActuationInterfacePanel` restyled to Cursor dark theme; `AimingAccessoryPanel.svelte` deleted (legacy 7-line shim). Calibrate destination now renders as 5-step wizard; Engage destination gated by 4-light safety interlock. Hotkey system (1-4 destinations, ? cheatsheet). Command palette Ctrl+K. Models + Settings drawers from left rail. Prior: Phase 5C COMPLETE. New: `core/vision_pipeline/.../optical_flow_sampler.{hpp,cpp}`, `core/transport/.../wire_format.hpp`, `hardware_sinks.hpp`, `ActuatorToEgressQueue`/`BgOdometryWireFrame` in `spsc_links.hpp`, six-thread `main.cpp`; dashboard `lib/components/panels/*`, `lib/exporter/jsonl_writer.svelte.ts`, `lib/profiles/{schema,scenarios,panel_registry,scenario_store}.ts`, `PreRecordModal.svelte`; tests `test_optical_flow_sampler.cpp`, `test_wire_format.cpp`, `test_hardware_sinks.cpp`. Prior: New files: `workspace_blueprint/PHASE_5C_HANDOFF.md` (single source of truth for Phase 5C execution with 24 atomic tasks T01–T24 across 7 themes, vocabulary discipline, sequencing, acceptance, recovery patterns); `.cursor/rules/250-phase-5c-execution.mdc` (binding execution rule pointing at the handoff doc, designed for smaller-model agents — 4.6 Opus / auto / composer). Prior state stands: Phase 6 part A landed (`core/main.cpp` rewritten with five threads — camera capture / inference / tracker / dashboard-egress / actuator-telemetry-emit, new `yolov8_detect` postprocessor + Catch2 coverage, `CameraFrame` + `CameraToInferenceQueue` SPSC alias, `output_sink.hpp` §11.4 NoOpSink stub, two reference manifests for the user's `C:/Users/heave/Desktop/onnx_collection/` — `coco-yolov8-baseline.yaml` for the 1×84×8400 COCO model + `person-only-yolov8.yaml` for the 1×5×8400 single-class line); Phase 5b dashboard scaffolding for synthesis v2+v3+v4+v5 (two new SoA pools `actuator_pool.svelte.ts` + `bg_odometry_pool.svelte.ts`, wire-decoder version-byte dispatcher with v1/v2/v3 decoders + self-tests, three-stream mock frame source, rewritten Cursor rule `200-dashboard-telemetry.mdc` binds v3+v4+v5, rewritten `dashboard/architecture_plan.md` v2); AC research deliverable `anticheat_data_exposure_2026.md` (550 lines, 31 cited primary sources, strictly defensive analytical research; extends dashboard menu architecture §12.2 with 25 derived features F01–F25, 8 dashboard panels P1–P8, 13 risks, 13 numbered recommendations including BEACON-compatible folder layout R4+R6 and Ed25519 attestation R9).

```
roc-ai-vision/
├── .cursor/
│   ├── rules/
│   │   ├── 000-roc-global.mdc
│   │   ├── 100-core-kinematics.mdc
│   │   ├── 200-dashboard-telemetry.mdc        (Phase 5b — rewritten to bind synthesis v2+v3+v4+v5: defensive-research framing non-negotiable, 3-zone shell + cardinality-of-4-destinations lock, composite Operate canvas layer order through Layer 1.5, three-pool data layer separation rules, wire-format version dispatch table, rate decoupling 30 Hz / 500 Hz / human-speed / RAF, OperatorCommand kind enum incl. Phase 5c additions, REPLAY-mode safety lock, render-optimisation reuse rules, sanitised-vocabulary lint, .svelte.ts naming convention)
│   │   ├── 220-operate-canvas-stage.mdc       (Binding overlay-alignment rule, glob `dashboard/**/*.svelte`: any component overlaying SVG/Canvas on top of `<img>`/`<video>` MUST use the aspect-ratio stage-div pattern from research_svg_overlay_alignment_2026.md; SVG overlay user coords MUST stay within `[0, VIEW_W] × [0, VIEW_H]` or be capped at half-min-dimension / clipped via `<clipPath>`; strokes MUST use `vector-effect="non-scaling-stroke"`)
│   │   ├── 250-phase-5c-execution.mdc         (Phase 5C — binding execution rule for smaller-model agents pointing at PHASE_5C_HANDOFF.md; vocabulary discipline, session-sizing, Living Blueprint update cadence, cross-reference table for fast lookup, out-of-scope deferrals to Phase 6+/Phase 7/Phase 1.5)
│   │   └── 300-research-standards.mdc
│   └── skills/
│       ├── roc-ekf-context/
│       │   └── SKILL.md
│       ├── roc-wire-protocol/
│       │   └── SKILL.md
│       ├── roc-onnx-inference/
│       │   └── SKILL.md
│       └── roc-research-methodology/
│           └── SKILL.md
├── workspace_blueprint/
│   ├── project_tree.md                          ← you are here
│   ├── roadmap.md
│   ├── architecture.md
│   ├── PHASE_5C_HANDOFF.md                      (single-source-of-truth Phase 5C execution handoff: §0 vocabulary discipline + §1 state at handoff + §2 ordered task list T01–T24 across 7 themes A/B/C/D/E/F/G + §3 sequencing diagram + §4 phase-completion acceptance + §5 recovery patterns + §6 unlocked-after-phase notes; designed for one composer-auto session per task ID)
│   └── research_notes/
│       ├── phase_1_inference_and_targeting.md   (ONNX intro + letterbox math + target-segment maps; foundational, partially superseded by the EP addendum)
│       ├── phase_1_onnx_gpu_core.md             (CUDA/TRT EP deep-dive; PARTIALLY STALE — kept as ORT API reference, but the production EP selection logic is in phase_1_universal_ep_and_manifest.md)
│       ├── phase_1_universal_ep_and_manifest.md (DirectML/OpenVINO/CPU EP probe-and-fallback; model-manifest YAML sidecar spec; YOLO26m NMS-free decoder; OSNet-AIN crop preprocessing — IMPLEMENTED in core/vision_pipeline/)
│       ├── phase_1_ensemble_architecture.md     (PINNED Phase 1.5 — multi-model SpatialAggregator with WBF + IoS; offline knowledge distillation paths; bias-variance ensemble math; integration plan for the user's 113-file ONNX collection at C:\Users\heave\Desktop\onnx_collection — deferred until single-model end-to-end demo green)
│       ├── phase_5_console_robotics_ml.md       (Dashboard menu research — robotics/ML vertical; 562 lines, 22 cited sources; Foxglove/Rerun/RViz/DeepStream/Isaac Sim/OpenVINO/Roboflow/MLflow/W&B/Hailo)
│       ├── dashboard_game_cv_overlay_ux_2026.md (Dashboard menu research — game-CV/overlay vertical; 410 lines, 24 cited sources; ReShade/OBS/Streamlabs/NVIDIA App/AMD Radeon/Razer Synapse/Logitech G HUB/Discord/Cheat Engine/MSI Afterburner/HUD theory/Foxglove)
│       ├── dashboard_pro_console_ux_2026.md     (Dashboard menu research — pro operator consoles vertical; 408 lines, 28 cited sources; Foxglove/RViz/DeepStream/Anduril Lattice/Skydio/QGC/OBS/ReShade/Datadog/Grafana/cvui/Tesla AP/FSD/Ultralytics/Milestone XProtect/Genetec/Palantir Gotham)
│       ├── anticheat_data_exposure_2026.md     (BIOMETRIC SECURITY RESEARCH — derived-feature catalogue & ML-ready export spec extending dashboard_menu_architecture_2026.md §12.2 JSONL schema. 550 lines, 31 cited primary sources, strictly defensive analytical research. §2 surveys 10 AC vendors (Riot Vanguard, BattlEye, EAC, VACnet, FACEIT, ESEA, XIGNCODE3, Denuvo, FACEIT Minerva, MS GameDVR); §3 surveys academic mouse-dynamics corpus 2007–2026 (Ahmed/Traore, Shen/Cai/Guan, Khan/Devlen/Manno/Hou survey, Wang 2025 LT-MAuthen, Siddiqui 2022 Wisconsin-Minecraft, Antal 2021 SapiMouse/SapiAgent, VADNet 2024); §4 catalogues 9 public datasets including Balabit, SapiMouse 120-user, BEACON 2026 HuggingFace multimodal; §5 specifies 25 derived features F01–F25 with mathematical definitions, complexity, human-vs-bot distributions, sub-sampling robustness, and cross-app transfer AUC; §6 implementation notes including C++ live-tier skeleton + windowed streaming algorithm; §7 visual rendering patterns; §8 LMM/foundation-model export including 224×224 CNN format + JSONL HuggingFace recipe + Python reference loader + multimodal vision-frame alignment via vision_frame_index binary search; §9 proposes 8 dashboard panels (P1-P8); §10 analyses 13 risks; §11 13 numbered recommendations including R1 8-feature live tier @ 500 Hz, R2 17-feature offline tier, R4 JSONL schema extension, R5 CNN PNG format, R6 multimodal alignment, R7 top-3 panels (P3 FittsResidualPanel, P7 SubPixelPanel, P1 VelocityProfilePanel), R8 HuggingFace publishing recipe, R9 Ed25519 attestation signing, R10 forbidden-vocabulary lint expansion, R11 sub-pixel verification in Calibrate wizard, R12 cross-app transfer-learning recipe, R13 v0.1 → v1.0 dataset publication cadence. Zero offensive content, zero forbidden-domain citations. Suitable for peer-reviewed publication in IEEE TIFS or as a Vanguard-style technical blog post.)
│       └── dashboard_menu_architecture_2026.md  (BINDING SYNTHESIS v2 + AMENDMENT v3 §11 + AMENDMENT v4 §12 + AMENDMENT v5 §13 — ~1280 lines total. v2: 4 destinations Operate/Inspect/Calibrate/Engage + 2 cross-cutting drawers + layered Operate canvas + Scenario×Profile preset system + Replay-as-mode-swap + Engage 4-light safety interlock + Calibrate 5-step wizard + Ctrl+K palette. v3 §11: Layer 1.5 actuator-path ring buffer + traditional aim sliders (FOV/α/deadzone/velocity/accel curves) + per-class ESP targeting-anchor selectors + dual-use OutputSink adapter + sanitized-vocabulary CI lint + REPLAY hard-disable. v4 §12: MouseDynamicsInspector featured panel in Inspect destination with side-by-side clean/dirty trace + 4 render modes (c/d/t/s hotkeys) + JSONL ML-ready export schema (session header + per-sample 380-byte event @ 500 Hz + per-window 720-byte feature event @ 5 Hz with 15+ derived metrics) + live slider feedback contract via OperatorCommand kSetSmoothingAlpha/etc + decoupled 500 Hz actuator-telemetry channel with new third orchestrator thread + 2 new AC Research Scenarios + 3 new panels + Record/Export UI strip with mandatory pre-record label modal + 5 risk additions B1-B5 + ~700 MB/hour gzipped session footprint. v5 §13: Third forensic channel — background optical-flow odometer (sparse Lucas-Kanade + cv::estimateAffinePartial2D rigid 3-DOF + center-mask exclusion, ~1-3ms/frame on 1080p CPU) — new version=3 BgOdometry wire frame (32 B @ 30 Hz = ~960 B/s) + SPSC fanout from camera capture (cv::Mat refcount makes duplicate cheap) + new SpectatorTelemetryMatrix panel with dual-line actuator/bg-velocity chart + cross-correlation strip with human/automation bands + 6 KPI feature-event additions including bg_correlation_lag_frames + bg_amplitude_ratio + bg_phase_coherence + bg_snap_event_count + 7 risk additions C1-C7 + robotics dual-use = visual-odometry SLAM front-end via Scenario label-map relabel. Cardinality of 4 destinations PRESERVED throughout all amendments.)
│       ├── phase_2_ekf_kinematics.md            (CA-EKF + DeepSORT mathematical contract; binding spec for the kinematics_engine implementation)
│       ├── phase_5_telemetry_ui.md              (Binary WS schema, zero-GC Svelte store, 144Hz SVG render architecture)
│       ├── target_prediction_ai.md              (target prediction research)
│       ├── research_sota_mot_trackers_2026.md   (SOTA MOT tracker comparison: ByteTrack, BoT-SORT, OC-SORT, Deep OC-SORT, StrongSORT++, Hybrid-SORT, MOTR family; benchmark tables + actionable integration recs)
│       ├── research_ipc_middleware_2026.md       (Phase 4 deep-dive: ROS 2 vs iceoryx vs ZeroMQ vs SPSC; hybrid IPC architecture decision)
│       ├── research_reid_embeddings_2026.md     (Re-ID backbone selection: OSNet-AIN vs ResNet-IBN vs CLIP; BoT tricks; ONNX quantization; gallery management)
│       ├── research_detection_models_2026.md    (YOLO26/RT-DETR/Co-DETR architecture comparison; hierarchical detection strategy; ONNX export; training pipeline)
│       ├── research_mpc_actuator_control_2026.md (Phase 6 — Linear MPC + OSQP + Ruckig jerk-limited smoothing; PBVS architecture; Dynamixel serial; latency compensation; safety constraints; GRU feedforward integration)
│       ├── research_dashboard_tech_2026.md      (2026 deep-dive: WebTransport vs WS, Canvas/SVG/WebGPU rendering, Svelte 5 runes zero-GC, SAB+Atomics, Foxglove/Rerun architecture, charting libs, SW caching, profiling methodology)
│       └── research_svg_overlay_alignment_2026.md (Canonical web pattern for source-pixel-perfect SVG bbox overlay on letterboxed `<img>`/`<video>` (`object-fit: contain`): aspect-ratio stage div + `viewBox="0 0 W H"` + `preserveAspectRatio="xMidYMid meet"` + `overflow: hidden`, no JS resize listener required. Surveys CVAT / Foxglove / Label Studio / Roboflow InferenceJS / Sunone Aimbot architectures; documents MJPEG-vs-`<video>` pitfalls (`onload` does not fire per frame, `videoWidth`/`videoHeight` post-`loadedmetadata`, `requestVideoFrameCallback` for frame-aligned overlay draws, CORS-free composition because `<img>` and `<svg>` are separate elements); rejects Option B (JS `getBoundingClientRect` measurement) on layout-read-tax + one-frame-lag grounds.)
├── core/
│   ├── CMakeLists.txt                          (Top-level aggregator: kinematics_engine + vision_pipeline + transport; builds `roc_vision.exe` orchestrator at C++20 from main.cpp; Phase 6 part A adds OpenCV core+imgproc+videoio to the orchestrator link surface for cv::VideoCapture)
│   ├── main.cpp                                (Phase 6 part A end-to-end orchestrator: cv::VideoCapture(idx) camera_capture_thread → inference_thread (roc::vision::InferenceEngine) → tracker_thread (roc::kinematics::Tracker) → dashboard_egress_thread (WsEgressServer) + §12.4 actuator_telemetry_emit_thread at 60 Hz emitting version=2 36-byte wire frames through a §11.4 NoOpSink stub. SIGINT graceful shutdown in reverse-startup order. CLI additions: `--camera IDX` / `--camera-width PX` / `--camera-height PX` / `--model PATH.onnx` (or $ROC_MODEL_ONNX) / `--manifest PATH.yaml` (default derived from model) / `--no-actuator-telemetry`)
│   ├── vision_pipeline/
│   │   ├── CMakeLists.txt                       (C++17 + FetchContent ORT 1.19.2 CPU prebuilt + Eigen 3.4 + Catch2 v3.5.4; system find_package OpenCV 4.13 + yaml-cpp 0.8.0; _stdcall→__stdcall macro shim for MinGW + ORT headers; copies onnxruntime.dll into build/; conditional ROC_VISION_HAVE_DML_EP / ROC_VISION_HAVE_OPENVINO_EP / ROC_VISION_BUILD_CUDA_EP)
│   │   ├── scripts/
│   │   │   └── fetch_yolo26n.py                 (Configure-time helper: downloads YOLO26n COCO .onnx into models/, writes reference yolo26n-coco.yaml / yolo26m-roc-humanoid.yaml / osnet-ain-x1-0.yaml manifests; non-fatal on network failure — tests skip-with-message at runtime)
│   │   ├── include/
│   │   │   └── roc/
│   │   │       └── vision/
│   │   │           ├── types.hpp                       (Re-export of roc::kinematics::Detection / Embedding / Label / Pixel + EpKind / EpProbeResult / ImageSize / LetterboxParams / ModelPath / Span<T> C++17 shim)
│   │   │           ├── manifest.hpp                    (§3.3 POD: InputSpec, OutputSpec, NormalizationSpec, PreprocessingSpec, RuntimeHints, ClassMapping; OutputHead/Format enums; label_from_string)
│   │   │           ├── manifest_loader.hpp             (ManifestLoader::load + load_from_string; no-throw, returns optional + error string)
│   │   │           ├── ep_probe.hpp                    (§1.2 probe-and-fallback; runtime DML/OV/CUDA availability flags)
│   │   │           ├── letterbox.hpp                   (§4.2 detection letterbox + §5.1 ReID stretch; preprocess + preprocess_batch + unletterbox)
│   │   │           ├── postprocessor.hpp               (§4.3 abstract strategy + PostprocessContext)
│   │   │           ├── postprocess/
│   │   │           │   ├── yolo26_detect.hpp           (§4.1 NMS-free decoder)
│   │   │           │   ├── yolov8_detect.hpp           (Phase 6 part A — Ultralytics YOLOv8 decoder; scores already sigmoid'd in graph; external per-class NMS; supports both [1,5,N] single-class and [1,84,N] 80-class layouts)
│   │   │           │   └── osnet_reid.hpp              (§5.2 L2-normalised 128-D + optional 512→128 projection)
│   │   │           ├── postprocessor_registry.hpp     (String-keyed strategy table)
│   │   │           └── inference_engine.hpp            (§6 orchestrator — owns Ort::Session + Manifest + LetterboxPreprocessor + Postprocessor*; noexcept hot path)
│   │   ├── src/
│   │   │   ├── manifest.cpp                            (label_from_string)
│   │   │   ├── manifest_loader.cpp                     (§3.4 schema validation chain via yaml-cpp; six rejection categories)
│   │   │   ├── ep_probe.cpp                            (DML/OV/CUDA append_provider gated by compile-time defines; CPU floor unconditional)
│   │   │   ├── letterbox.cpp                           (cv::resize + cv::copyMakeBorder + per-channel ImageNet normalisation; NCHW/NHWC packing)
│   │   │   ├── postprocess/
│   │   │   │   ├── yolo26_detect.cpp                   (Single-pass channel-major decode; sigmoid + argmax + §4.2 inverse letterbox)
│   │   │   │   ├── yolov8_detect.cpp                   (Phase 6 part A — Ultralytics-export decoder; handles both [1,5,N] single-class and [1,84,N] 80-class layouts; per-class greedy NMS with manifest-configurable iou threshold; scores read as already-sigmoid'd; ~190 LOC including header docstring)
│   │   │   │   └── osnet_reid.cpp                      (Optional projection + L2 normalisation with epsilon guard)
│   │   │   ├── postprocessor_registry.cpp              (Built-in heads pre-registered)
│   │   │   └── inference_engine.cpp                    (Manifest load → EP probe → Ort::Session → introspect → run/run_batched with noexcept hot path)
│   │   └── tests/
│   │       ├── test_manifest_loader.cpp                (§3.4 — 11 cases covering valid + every rejection category)
│   │       ├── test_ep_probe.cpp                       (CPU EP universality + 65-B embedded Identity ONNX fixture)
│   │       ├── test_letterbox.cpp                      (forward+inverse round-trip <1 px; padding gray value; batched ReID stretch; degenerate empty input)
│   │       ├── test_yolo26_decode.cpp                  (synthetic [1, 4+C, N] tensors with hand-placed peaks; below-threshold gating; rank-mismatch graceful return)
│   │       ├── test_yolov8_decode.cpp                  (Phase 6 part A — coverage for `Yolov8DetectPostprocessor`: 5xN single-class score round-trip, 84xN COCO argmax + class lookup, per-class greedy NMS suppresses overlapping anchors, malformed-rank graceful return, inverse letterbox round-trip; 6 cases / 25 assertions)
│   │       ├── test_osnet_decode.cpp                   (128-D + 512→128 projection + zero-vector epsilon + rank-4 [B,D,1,1] acceptance)
│   │       ├── test_inference_engine.cpp               (End-to-end YOLO26n COCO load + noise-frame run + missing-model rejection)
│   │       └── test_byte_compat.cpp                    (std::is_same<vision::Detection, kinematics::Detection> static asserts + Label wire-value pins)
│   └── kinematics_engine/
│       ├── CMakeLists.txt                       (Eigen 3.4 + Catch2 v3 via FetchContent; Release flags -O3 -march=native -fno-math-errno -fno-trapping-math, no -ffast-math)
│       ├── include/
│       │   └── roc/
│       │       └── kinematics/
│       │           ├── types.hpp                       (§1.2 binding contract: State 9×1, StateCov 9×9, Meas 4×1, Embedding 128×1, Label enum, TrackId, Detection, CameraPose; clamp_dt + symmetrize helpers)
│       │           ├── object_priors.hpp               (§3.2 anthropometric W₀,H₀ table; §3.11.1 anatomical μ_S, σ_S; default R per §3.6; Q_c per §2.4)
│       │           ├── ca_ekf.hpp                      (§2.1 closed-form F(Δt); §2.2 closed-form CWNJ Q(Δt); CaEkf class with Joseph-form full / 2-row / 6-row / 3-row updates)
│       │           ├── pinhole_measurement.hpp         (§3.4 H derivation, §3.5 axis-aligned oracle, §3.10.3 four-case selective-axis init)
│       │           ├── regime_detector.hpp             (§2.6.1 EMA-NIS, §2.6.2 ρ piecewise-linear; init at η̄₀ = 4)
│       │           ├── track.hpp                       (Track aggregate + EmbeddingGallery fixed-capacity ring; lifecycle counters; boundary probation flags; hierarchy-violation flag)
│       │           ├── association.hpp                 (§4 cosine GEMM, Mahalanobis χ²₄ Cholesky gate, gated combined cost, matching cascade, IoU fallback, iou_xywh helper)
│       │           ├── lap_jv.hpp                      (rectangular dense JV/Hungarian SAP solver; sentinel kForbiddenCost = 1e6)
│       │           ├── hierarchical_constraint.hpp     (§3.11 two-pass Gauss–Seidel coupled update + §3.11.4 plausibility gate + EventCallback for hierarchy_violation)
│       │           ├── target_selector.hpp             (multi-target ranking + EMA-smoothed (dx, dy); operator lock override)
│       │           ├── pending_spawn_buffer.hpp        (§3.10.3 DEFERRED detection holding pen, T_hold = 5)
│       │           ├── wire_format.hpp                 (§7 binary frame: 12 B header + 86 B/track + decoder; status-flag bit constants)
│       │           └── tracker.hpp                     (top-level Tracker orchestrating §5 per-frame pipeline)
│       ├── src/
│       │   ├── association.cpp
│       │   ├── ca_ekf.cpp                              (joseph_update<M> templated kernel)
│       │   ├── hierarchical_constraint.cpp
│       │   ├── lap_jv.cpp                              (Hungarian / shortest-augmenting-path with column potentials)
│       │   ├── pending_spawn_buffer.cpp
│       │   ├── pinhole_measurement.cpp
│       │   ├── regime_detector.cpp                     (anchor TU only; methods inlined in header)
│       │   ├── target_selector.cpp
│       │   ├── track.cpp                               (anchor TU only)
│       │   ├── tracker.cpp                             (predict → assoc → update → shrinkage → constraint → probation → lifecycle → wire-egress, IN ORDER)
│       │   └── wire_format.cpp
│       └── tests/
│           ├── test_association.cpp                    (cosine GEMM ≡ naïve loop, χ² gate, LAP-JV vs brute force, cascade age ordering)
│           ├── test_ca_ekf.cpp                         (F closed form, Q PSD/symmetric, no-noise CA trajectory, Joseph symmetry, v/a shrinkage)
│           ├── test_hierarchy.cpp                      (GS geometric convergence ratio + plausibility gate + apply() walk)
│           ├── test_pinhole.cpp                        (FD vs analytic Jacobian, axis-aligned oracle, four §3.10.3 cases, Z-floor)
│           ├── test_regime.cpp                         (EMA convergence, ρ breakpoints, regime transitions, monotonicity)
│           ├── test_tracker.cpp                        (5-track / 200-frame e2e w/ synthetic occlusion → ID-switch=0, lifecycle, wire-frame size)
│           └── test_wire_format.cpp                    (12 B / 86 B layout assertions, round-trip, truncation rejection, empty frame)
│   └── transport/                                  (Phase 4 — hybrid IPC layer: SPSC ring buffers in-process + IXWebSocket binary egress to the dashboard; per research_ipc_middleware_2026.md §8 architecture / §10 decision record)
│       ├── CMakeLists.txt                          (FetchContent rigtorp/SPSCQueue v1.1 + IXWebSocket v11.4.5 — Apache-2.0 single-CMakeLists library; uWebSockets fallback per §6.3 deviation; links roc::kinematics PUBLIC, OpenCV core PUBLIC, ixwebsocket PRIVATE; C++17 strict for the library, the test driver bumps to C++20 because the smoke test calls Tracker::step directly)
│       ├── include/
│       │   └── roc/
│       │       └── transport/
│       │           ├── spsc_links.hpp                  (Six typed rigtorp::SPSCQueue aliases: FrameToInferenceQueue / InferenceToTrackerQueue / TrackerToActuatorQueue / TrackerToWireQueue / OperatorCommandQueue / CameraToInferenceQueue (Phase 6 part A — capacity 4, payload `CameraFrame { cv::Mat frame; capture_ts_ns; frame_seq }` aligned to 64 B for §5.4 false-sharing); cache-line-aligned payload structs FrameBatch / CameraFrame / DetectionBatch / ActuatorCommand / WireFrame; capacities 4 / 16 / 8 / 4 / 16 / 4)
│       │           ├── operator_command.hpp            (Reverse-channel OperatorCommand POD + 22-byte little-endian wire format; LockTarget / ReleaseLock / UpdateSelectorWeights kinds; serialize/deserialize allocation-free + noexcept)
│       │           ├── origin_check.hpp                (Header-only constexpr loopback enforcement: is_loopback_origin_header + is_loopback_peer_address + combined gate; accepts localhost / 127.0.0.1 / [::1] / api.roc.internal across http/https/ws/wss)
│       │           ├── output_sink.hpp                 (Phase 6 part A §11.4 stub — abstract `OutputSink` strategy + `NoOpSink` concrete; `OutputSinkKind` u16 enum mirrored into the §12.4 v2 wire frame's `sink_kind` field; `ActuatorEmission` POD uses sanitised vocabulary `command_u_px`/`delta_u_per_tick` per §11.6 risk A3 forbidden-vocabulary lint)
│       │           └── ws_egress.hpp                   (WsEgressServer class — pimpl façade over IXWebSocket; broadcast(bytes) is thread-safe; start/stop RAII; CommandCallback hook for the reverse channel; listen-port reporter; client_count diagnostic)
│       ├── src/
│       │   ├── operator_command.cpp                    (LE write_le/read_le helpers + reserved-byte + kind enumerator validation)
│       │   └── ws_egress.cpp                           (IXWebSocket pimpl: setOnConnectionCallback / Origin+peer gate at the Open event / approved_clients set; broadcast snapshots the client list and copies the payload string once; graceful shutdown closes every approved client before stopping the accept thread)
│       └── tests/
│           ├── test_spsc_links.cpp                     (Cache-line alignment static_asserts; FrameBatch / DetectionBatch / WireFrame / ActuatorCommand round-trips; capacity-overflow returns false; threaded producer/consumer 4096-message stress test)
│           ├── test_origin_check.cpp                   (Peer / origin / combined-gate tables across approved + hostile inputs; case-insensitive scheme + host; suffix-attack rejection)
│           ├── test_operator_command.cpp               (22-byte size guard; round-trip every kind; explicit hex test vector for LockTarget; truncated / unknown-kind / non-zero-reserved rejection)
│           ├── test_ws_egress.cpp                      (Server lifecycle; loopback client → broadcast → byte-exact receipt; operator-command callback dispatch; hostile-Origin client kicked at handshake; broadcast on stopped server returns 0)
│           └── test_orchestrator_smoke.cpp             (2-second full-pipeline bring-up: synthetic detection generator + tracker + WS egress + loopback client → asserts ≥1 wire frame received client-side, no thrown exceptions, clean thread teardown; compiled C++20 for std::span)
├── ros2_ws/                                     (DEPRECATED 2026-05-17 — pending physical removal per research_ipc_middleware_2026.md §10 Decision Record; left on disk for the user to delete manually after Phase 4 acceptance; the transport layer at core/transport/ now occupies the IPC role)
│   └── src/
│       ├── tracker_interfaces/
│       │   ├── package.xml
│       │   ├── CMakeLists.txt
│       │   └── msg/.gitkeep
│       └── roc_vision_nodes/
│           ├── package.xml
│           ├── CMakeLists.txt
│           ├── launch/.gitkeep
│           ├── config/.gitkeep
│           └── src/.gitkeep
├── dashboard/
│   ├── architecture_plan.md                     (v2 — Phase 5/6 actionable plan derived from dashboard_menu_architecture_2026.md v2+v3+v4+v5 synthesis. Documents 3-zone shell, 4 destinations w/ forward+current name mapping, featured panels per destination including MouseDynamicsInspector + SpectatorTelemetryMatrix, three hot-path pools (trackPool / actuatorPool / bgOdometryPool) with rates + memory budgets, wire-format version-byte dispatch table, OperatorCommand reverse channel with new kSetSmoothingAlpha / kSetNisDeadbandScaling / kSetFovRadius / kSetTargetingAnchor kinds, JSONL exporter Phase 5c work, Phase 5b vs 5c vs 6+ work split table, component boundary rules, SSR-disabled posture lock, in-flight dependencies)
│   ├── package.json                             (SvelteKit 2 + Svelte 5 runes)
│   ├── svelte.config.js
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── src/
│   │   ├── app.html
│   │   ├── app.d.ts
│   │   ├── routes/
│   │   │   ├── +layout.svelte
│   │   │   └── +page.svelte                     (mounts NavigationShell)
│   │   └── lib/
│   │       ├── stores/
│   │       │   ├── aiming_config.svelte.ts      ($state — smoothing / deadband / profile)
│   │       │   ├── aiming_config.ts
│   │       │   ├── hardware_config.svelte.ts    ($state — serial / baud / crop / diagnostic ping)
│   │       │   ├── hardware_config.ts
│   │       │   └── track_pool.svelte.ts         (Phase A — SoA 64-slot pool mirroring wire_format.hpp §7.2; 4 $state runes for non-canvas consumers; ~14 KB typed-array backing store)
│   │       ├── telemetry/
│   │       │   ├── wire_decoder.ts              (Phase A — pure DataView byte walker, zero per-frame alloc; DEV-only verify_layout() against real TrackPool)
│   │       │   ├── mock_frame_source.ts         (Phase A — 30 Hz binary frame synth, 6 simulated tracks w/ boundary/stationary/hierarchy_violation toggles, two-pass parent/child hierarchy, fake-metre p_world)
│   │       │   └── telemetry_socket.ts          (Phase 4 — live binary WebSocket client → ws://127.0.0.1:8765; 22 B OperatorCommand encoder; exponential reconnect 1→8 s; reactive ConnectionState rune; canvas auto-falls-back to mock_frame_source when live disconnects)
│   │       ├── domains/
│   │       │   └── controller_matrix.ts         (4-domain IDs + exact labels)
│   │       └── components/
│   │           ├── NavigationShell.svelte       (4-Domain Controller Matrix; spatial-only TelemetryCanvas mount)
│   │           ├── TelemetryCanvas.svelte       (Phase A + Phase 4 follow-on — three-layer SVG with will-change isolation, 64 pre-created rect/text/path refs, RAF imperative attribute mutation, status-flag visual mapping + 3-frame hierarchy_violation decay; SRC indicator in status strip; live-priority frame ingest from telemetry_socket with mock_frame_source fallback)
│   │           ├── domains/
│   │           │   ├── DomainPanelLayout.svelte (shared panel chrome + expansion slot)
│   │           │   ├── ObserverMatrixPanel.svelte
│   │           │   ├── KinematicTrajectoryControllerPanel.svelte
│   │           │   └── HardwareActuationInterfacePanel.svelte
│   │           └── tabs/
│   │               └── AimingAccessoryPanel.svelte (legacy re-export → kinematic panel)
│   └── static/
│       └── robots.txt
├── models/
│   ├── .gitkeep
│   ├── yolo26n-coco.onnx                        (9.94 MB; fetched by core/vision_pipeline/scripts/fetch_yolo26n.py at configure time when ROC_VISION_FETCH_TEST_MODELS=ON; integration-test fixture)
│   ├── yolo26n-coco.yaml                        (Reference manifest §3.3 — bring-up smoke test for the engine)
│   ├── yolo26m-roc-humanoid.yaml                (Reference manifest §3.3 — production 6-class humanoid target; .onnx requires custom training)
│   ├── osnet-ain-x1-0.yaml                      (Reference manifest §3.3 — OSNet-AIN x1.0 256×128 ImageNet-normalised ReID extractor)
│   ├── coco-yolov8-baseline.yaml                (Phase 6 part A — points at `C:/Users/heave/Desktop/onnx_collection/Red Dead Redemption II (REDM) by Padre.onnx`, the alphabetically-first of the two [1,84,8400] 80-class COCO models in the user's collection; head=yolov8_detect, format=cxcywh_sigmoid; only the four humanoid-relevant COCO classes (person, backpack, handbag) map to roc::kinematics::Label values, the remaining 76 stay Unknown and the tracker filters them on spawn)
│   └── person-only-yolov8.yaml                  (Phase 6 part A — points at `C:/Users/heave/Desktop/onnx_collection/1.5kR6.onnx`, the alphabetically-first of the 82% subset of [1,5,8400] single-class detectors in the user's collection; head=yolov8_detect, format=cxcywh_sigmoid; class 0 maps to roc::kinematics::Label::Person)
├── AGENTS.md                                   (Top-level agent memory file for future Cursor sessions + external tooling (Codex, Claude Code, etc.): project one-liner pointing at the binding spec, non-negotiable discipline list, quick map to the 8 most-referenced files, and a single-line current-state pointer at `workspace_blueprint/project_tree.md`. Practical, ≤60 lines, no philosophy.)
├── creation-idea.txt
├── cursor.md
└── initial work prompt.md
```
